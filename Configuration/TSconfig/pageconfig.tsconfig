mod{
    qcComments {
        technicalProblems {
          // Order by comment date
          orderType = DESC
          maxRecords = 50
          numberOfSubPages = 50
          showRecordsForHiddenPages = 1
          enableFixButton = 1
        }
        comments {
            // Order by comment date
            orderType = DESC
            maxRecords = 100
            numberOfSubPages = 50
            showRecordsForHiddenPages = 1
            enableDeleteButton = 1
        }
        statistics {
            maxRecords = 30
            showRecordsForHiddenPages = 0
        }

        csvExport {
            filename {
                dateFormat = Ymd
            }
            separator = ;
            enclosure = "
            escape = \
        }
    }
}
